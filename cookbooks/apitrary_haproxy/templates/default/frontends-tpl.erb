acl is_www_clear hdr_beg(host) -i apitrary
use_backend www_cluster if is_www_clear
acl is_www2 hdr_dom(host) -i www2
use_backend www_cluster if is_www2
acl is_www hdr_dom(host) -i www
use_backend www_cluster if is_www