# Defaults for cookbooks.supervisor initscript
# sourced by /etc/init.d/cookbooks.supervisor
# installed at /etc/default/cookbooks.supervisor by the maintainer scripts

#
# This is a POSIX shell fragment
#

# Additional options that are passed to the Daemon.
DAEMON_OPTS="<%= @node['cookbooks.supervisor']['daemon_options'] %>"
